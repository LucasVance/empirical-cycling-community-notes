source "https://rubygems.org"

# This gem installs Jekyll and all the dependencies that GitHub Pages uses to build your site.
# Using it ensures that your local preview will match what you see on GitHub.
gem "github-pages", group: :jekyll_plugins